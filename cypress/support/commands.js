import SESSIONSTORAGE_KEYS from "../../src/modules/shared/constants/sessionStorage";

Cypress.Commands.add('auth', (username, password) => {
	// add authentication flow
	//sessionStorage.setItem(SESSIONSTORAGE_KEYS.TEMP_ACCESS_TOKEN, token);
});
